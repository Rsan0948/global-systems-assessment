__pycache__/
*.pyc
.pytest_cache/
.venv/

# HydroRIVERS raw downloads (60+ MB each); derived Rb caches in results/ are committed
studies/2C_river_networks/data/

# NCBI taxdump raw download (~75 MB); derived fan-out cache in results/ is committed
studies/4_negative_controls/data/

# DGS raw source downloads (Harvard ECI, V-Dem, World Bank, UCDP); panel cache in results/ is committed
studies/2A_political_fragmentation/data/

# MI pipeline: data inputs and generated outputs ARE committed (see mi_pipeline/DATA_PROVENANCE.md).
# (Previously git-ignored; convention reversed 2026-06-28 — the real panel lives in the repo.)
